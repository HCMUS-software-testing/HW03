# Docs Reading Rules

- Khi cần đọc tài liệu trong thư mục `docs/`, ưu tiên đọc các file Markdown (`*.md`).
- Chỉ mở hoặc trích xuất PDF trong `docs/` khi không có bản Markdown tương ứng hoặc khi người dùng yêu cầu rõ ràng, vì đọc PDF tốn token hơn.
